# Cursor IDE Integration

## Configuration

Add to Cursor settings:

```json
{
  "mcp.servers": {
    "server-name": {
      "command": "python",
      "args": ["-m", "package_name", "mcp_server"]
    }
  }
}
```

## Features

- Tool discovery
- Parameter autocomplete
- Result display
