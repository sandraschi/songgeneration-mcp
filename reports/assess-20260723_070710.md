# Assessment Report: songgeneration-mcp

**Date**: 2026-07-23 07:07:10
**Phase**: 1 — Assess (Read-Only)
**Repo**: D:\Dev\repos\songgeneration-mcp

---

## Summary

| Category | Score |
|----------|-------|
| CRITICAL | 5 |
| HIGH | 4 |
| MEDIUM | 3 |
| LOW | 1 |
| OK | 8 |

---

## Detailed Findings

### 1. pyproject.toml — HIGH

- **Author**: `{name = "MCP Studio"}` — should be `sandraschi` or include correct email
- **URLs**: Only `Repository` is set; missing `Homepage`, `Issues`, `Documentation` — but the existing URL does point to `sandraschi`
- **Entry point**: MISSING — no `[project.scripts]` section. The server cannot be installed as a CLI tool
- **Ruff config**: Present with line-length 120, py312 target, select rules
- **Dependencies**: `fastmcp>=3.4.4`, `prefab-ui>=0.14.0` — SOTA-compliant

### 2. justfile — OK

- Has `serve`, `serve-mcp`, `test`, `lint`, `fix`, `install-all`, `install-minimal`
- Imports `scripts/just/fleet.just` for shared recipes
- Uses `uv run` consistently — good

### 3. .gitignore — MEDIUM

- Covers: `__pycache__/`, `dist/`, `*.egg-info/`, `.venv`, `*.bak`, `*.bak.*`
- **MISSING**: `node_modules/` — not listed, web_sota node_modules are unprotected
- Missing: `reports/` — no entry for assessment output directory

### 4. README.md — OK

- Hero badges: Python 3.12+, FastMCP 3.4+, License
- Quick start with `uv sync` + `uv run uvicorn`
- Backend comparison table (Lyria, Stable Audio, MusicGen, Studio API)
- API docs, webapp section, companion MCP handoff note
- Links to docs/ sub-documents

### 5. AGENTS.md — CRITICAL

- Only 9 lines — barely more than a stub
- Missing: port numbers, key file map, reading order, tool categories, entry points
- Missing: dependency reading order (src/ structure, services, tools)
- Fails the fleet per-directory AGENTS.md standard

### 6. llms.txt — CRITICAL

- **FILE NOT FOUND**
- Required per fleet PACKAGING_STANDARDS §5

### 7. llms-full.txt — CRITICAL

- **FILE NOT FOUND**
- Required per fleet PACKAGING_STANDARDS §5

### 8. glama.json — HIGH

- **FILE NOT FOUND**
- Required for registry visibility
- Note: `manifest.json` exists at root but is a different format (old-style MCP manifest)

### 9. .env.example — HIGH

- **FILE NOT FOUND**
- Required to document all env vars without leaking real secrets

### 10. start.ps1 — CRITICAL

- **No zombie port clearing**: Does not use `Get-NetTCPConnection` + `Stop-Process` before binding
- **No health poll**: Does not poll backend for readiness after launch
- **Uses `npm run dev`** instead of `bun run dev` (inconsistent with README which says `bun install && bun run dev`)
- **No headless detection** for CI/auto-start scenarios
- **No keep-alive loop** to maintain the session
- **Launches backend with `Start-Process pwsh` hidden** — no process tracking

### 11. start.bat — OK

- Simple wrapper calling start.ps1 — adequate

### 12. CHANGELOG.md — OK

- Has "Unreleased" section with detailed entries
- Covers LeVo 2 / SG2 integration, DJ handoff, webapp features, Studio URL targeting
- Well-structured

### 13. PRD.md — OK

- Found at `docs/PRD.md` (not repo root, but acceptable)
- Comprehensive: goals, non-goals, competitive landscape (SG2 vs Lyria 3 Pro), positioning
- Includes cross-MCP handoff design

### 14. .pre-commit-config.yaml — MEDIUM

- **FILE NOT FOUND**
- Prevents consistent linting without relying on developer memory

### 15. .github/workflows/ci.yml — MEDIUM

- **Uses `pip`** instead of `uv` for installs — not fleet standard
- **Only triggers on `v*` tags** — no push/PR triggers for main branch
- **Uses Python 3.11** — fleet standard is 3.12+
- Runs lint + test, but limited scope

### 16. docs/ — OK

- Substantial: `BACKENDS.md`, `INSTALL.md`, `LYRIA_VS_SG2.md`, `PRD.md`, `MCP_SERVER_STANDARDS.md`, `MCPB_STANDARDS.md`, `CLIENT_RULEBOOKS/`
- Has `README.md` navigation within docs/

### 17. reports/ — CRITICAL

- **Directory MISSING** — just created by this assessment
- **`.gitignore` entry**: MISSING — `reports/` not in .gitignore
- **`.assess-fix-timestamp`**: MISSING
- **`.ghaudit-timestamp`**: MISSING

---

## Additional Findings

### Stale Backup Files — LOW

| File | Size | Last Modified |
|------|------|---------------|
| `pyproject.toml.20260717_120000.bak` | 1,189 B | 2026-07-02 |
| `start.ps1.bak.20260508_202256` | 825 B | 2026-04-20 |
| `start.ps1.bak.20260508_202351` | 825 B | 2026-04-20 |

3 stale `.bak` files present. Fleet policy says `.bak` files are gitignored but should be cleaned up after verification.

### .nopublish — OK

- File does NOT exist — repo can be published

### web_sota/ Frontend — OK

- `package.json` exists with `dev`, `build`, `lint`, `biome:ci` scripts
- Uses React 19, Vite 7, Tailwind 3, Biome 2 — SOTA stack
- `dist/` exists (built artifact)
- `biome.json` present for linting
- Port 10884 in vite.config.ts (matches WEBAPP_PORTS.md)

### native/ Tauri Wrapper — HIGH / CRITICAL

- **CRITICAL**: `tauri.conf.json` `bundle.resources` includes `"resources/.env"` — bundles the real `.env` file with personal API keys. Must use `.env.example` instead
- **HIGH**: `frontendDist` points to `../webapp/dist` but the actual frontend lives at `web_sota/` → `../web_sota/dist`
- `build.ps1` bundles `.env` (not `.env.example`) at line 71-78 — same security issue
- `hooks.nsh` present with correct process kill patterns
- `Cargo.toml` has standard Tauri 2.0 deps
- `backend.rs` appears present at `native/src/backend.rs`

### CI Pipeline — MEDIUM

- Uses `pip install -e ".[dev]"` instead of `uv sync`
- No `uv.lock` awareness in CI
- Missing push/PR triggers for pre-merge validation

---

## Score Summary

| # | Item | Score |
|---|------|-------|
| 1 | pyproject.toml | HIGH |
| 2 | justfile | OK |
| 3 | .gitignore | MEDIUM |
| 4 | README.md | OK |
| 5 | AGENTS.md | CRITICAL |
| 6 | llms.txt | CRITICAL |
| 7 | llms-full.txt | CRITICAL |
| 8 | glama.json | HIGH |
| 9 | .env.example | HIGH |
| 10 | start.ps1 | CRITICAL |
| 11 | start.bat | OK |
| 12 | CHANGELOG.md | OK |
| 13 | PRD.md | OK |
| 14 | .pre-commit-config.yaml | MEDIUM |
| 15 | .github/workflows/ci.yml | MEDIUM |
| 16 | docs/ | OK |
| 17 | reports/ + timestamps | CRITICAL |
| — | Stale .bak files | LOW |
| — | .nopublish | OK |
| — | web_sota/ builds | OK |
| — | native/ tauri.conf.json | CRITICAL |
| — | native/ bundles .env | CRITICAL |

**CRITICAL count: 5** (AGENTS.md, llms.txt, llms-full.txt, start.ps1, reports/ + native .env/config)
